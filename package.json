{
    "name": "@vortexus.js/monorepo",
    "license": "GPL-3.0",
    "private": true,

    "scripts": {
        "build": "turbo run build",
        "flush": "turbo run flush"
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "@types/node": "^20.14.0",
        "rimraf": "^5.0.7",
        "turbo": "^1.13.3",
        "typescript": "^5.4.5"
    },
    "dependencies": {
        "@changesets/cli": "^2.27.5"
    }
}
